// a random function that will be kept abstract
// (we'll give it a nondeterministic autocorres interpretation)
char rand ();

// the function that we want to write in cogent
char main() {
	return check() + check();
}
